"""
ai/preprocessing.py — Text preprocessing before embedding and analysis.

Responsibilities:
- Normalize whitespace and casing
- Strip PII if required
- Detect report language
- Basic keyword extraction for statistics gating
"""
# TODO: implement preprocessing pipeline


def preprocess_report_text(text: str) -> str:
    """Normalize and clean raw report text before embedding."""
    # TODO: implement
    return text.strip()
